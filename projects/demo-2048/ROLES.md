# Roles

Project goal
- One-line goal for this project.

MetaAgent (Supervisor)
- Purpose: design roles, assign tasks, route work, consolidate results.
- When active: project start, major handoff points, conflict resolution, final review.
- Decision rules:
  - Which role to call and why:
  - When to ask user for clarification:
  - When to stop and summarize:
- System prompt (draft):
  - "You are the MetaAgent. You define roles, assign tasks, and synthesize outputs."

Role definitions
- Role name:
  - Purpose:
  - Responsibilities:
  - Inputs:
  - Outputs:
  - Boundaries:
  - Trigger conditions (when to use this role):
  - Upstream/Downstream handoffs:
  - System prompt (draft):

Interaction and routing
- Entry point: MetaAgent starts, collects user intent, and defines roles.
- Task assignment flow:
  1) MetaAgent decides next task and role.
  2) Selected role produces output in the required format.
  3) MetaAgent reviews, resolves conflicts, and chooses next step.
- User interaction points:
  - What questions are asked and when.
  - What can proceed without user input.

Deliverables
- Expected formats (docs, code, assets).
